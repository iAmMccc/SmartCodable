
# 内部流程说明


## CGFloat的解析

#### `CGFloat` 是一个结构体（`struct`），而不是 `typealias`

```
@frozen public struct CGFloat {
    public typealias NativeType = Double // or Float on 32-bit
    public var native: NativeType
}
```

- 它本质上是一个**包装类型**：包装了 `Double`（在 64 位）或 `Float`（在 32 位架构）。
- 它提供了多个 `init(_ value: Float/Double/CGFloat)`，来方便你用普通浮点数构造。

进行解析时候，或享受`Date，Data等`同等的处理方式，被当初特殊基础解析数据类型处理。





## DecodingCache

解析存储，对应的容器级别。存储时，使用容器的codingPath作为标识。通过key查询对应的内容。比如值解析器，属性初始化值。

### 关于值解析器


### 关于属性初始化值
